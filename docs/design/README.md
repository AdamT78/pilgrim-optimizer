# Design records

This folder keeps records of design decisions whose reasoning cannot be reconstructed from the
shipped result. They are historical references, not outputs regenerated or compared by tests.
Interactive prototypes belong here too; a design record need not be a rendered comparison.

The `screenshots/` folder is the opposite: every file there is written by a test, and a test guards
that inventory in both directions.
