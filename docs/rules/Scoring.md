# Scoring

Current search evaluation uses a temporary placeholder:

`victory_points + piety + stone + silver + wheat`

This is not final Pilgrim scoring and exists only to support early deterministic search experiments.
