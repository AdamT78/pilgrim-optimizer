Five round stone floor tile for a medieval board game, seen from a low angle. Output: five images.

GEOMETRY — this is the part that must be exact:
The tile is a perfect circle lying flat on the ground. Seen from 32 degrees above
the horizon, a circle draws an ellipse whose height is sin(34) = 0.559 of its width.
This ratio applies to the PAVED SURFACE — the ellipse of flagstones inside the rim,
measured on its own. Do not include the rim's thickness when judging the ratio.
Get this ratio right before anything else.

PROJECTION:
Orthographic. The paved surface is square flagstones lying flat on that same ground
plane. Every flagstone is the same size — the ones at the back must NOT be smaller,
and the joint lines must NOT converge toward a vanishing point. Render as if shot
from very far away with a long telephoto lens, so perspective is flat.

THE RIM:
A low stone rim runs around the edge, about 5% of the tile's width thick, giving the
slab visible thickness along its near edge. The rim is a flat ring lying on the same
ground as the flagstones, so it is foreshortened exactly as they are: compress it
vertically by the same amount as the paved surface, so the rim appears much thinner
at the top and bottom of the ellipse than it does at the left and right. Do not draw
it as a ring of even thickness all the way round.

THE MEASURING RING:
Draw a single closed ellipse in pure spring green, RGB 0 255 128, as a thin line
around the tile — about 2% of the tile's width thick. It is a measuring mark, not
part of the scene: flat colour, no texture, no shading, no glow.

The ring must be the SAME ellipse the tile is: a circle lying flat on the same
ground, seen from the same camera, so its height is the same fraction of its width
as the tile's own paved surface.

The ring must be clearly larger than the tile and must not touch or pass behind it
anywhere — leave an obvious gap all the way round, with the ring about half again as
wide as the tile. The whole ring must be visible and unbroken.

THE OUTLINE MUST BE READABLE:
- Transparent background.
- No drop shadow, no glow, no soft haze, and no contact shadow spreading below the
  tile's near edge. Nothing at all below the bottom rim.
- Crisp edges. Do not feather, blur or fade the outline.
- The whole tile in frame, not cropped, roughly centred.
- The ellipse's long axis exactly horizontal — do not tilt or rotate the tile.
- Nothing standing on or in front of the tile. The tile alone.

LIGHTING AND STYLE:
Dark blue-grey riven slate, laid in irregular flags, grim and muted, 14th century
England. Light from the upper left. Nothing glossy or wet.
